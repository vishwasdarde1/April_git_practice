terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.0"   # pin to a stable version
    }
  }
}

provider "aws" {
  region  = "us-east-1"
  profile = "default"     # uses AWS CLI profile
}
